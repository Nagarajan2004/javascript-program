//Find sqrt of number

const num = 64;

const num1 = parseInt(prompt("enter the number :"));

const sqrtValue = ("sqrt of 64 :", Math.sqrt(num));
const result = Math.sqrt("sqrt of ",num1,":", Math.sqrt(num1));

console.log(sqrtValue, result);